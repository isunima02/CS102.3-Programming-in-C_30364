#include <stdio.h>
#include <stdlib.h>

int main()
{
    int caps;
    for ( caps = 65; caps < 91; caps++)
    {
        printf (" \n The ASCII value of %c is %d ", caps, caps);
    }

    return 0;
}
